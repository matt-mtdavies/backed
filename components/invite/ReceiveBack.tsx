"use client";
import { useState } from "react";import { currencySymbol } from "@/lib/money/currency";import type { InviteView } from "@/lib/invites/get-invite";import { BackedMark } from "@/components/brand/BackedMark";import { BackedLogo } from "@/components/brand/BackedLogo";
export function ReceiveBack({invite,token}: {invite:InviteView;token:string}){const [accepted,setAccepted]=useState(invite.accepted);const [busy,setBusy]=useState(false);const [shared,setShared]=useState("");const amount=invite.amountMinor/100;
  const accept=async()=>{setBusy(true);const response=await fetch(`/api/invites/${token}/accept`,{method:"POST"});setBusy(false);if(response.ok)setAccepted(true)};
  const share=async()=>{const url=`${location.origin}/p/${invite.promiseSlug}`;const data={title:`${invite.achieverName} is backed`,text:`Get behind ${invite.achieverName}: ${invite.promiseTitle}.`,url};if(navigator.share){await navigator.share(data);setShared("Shared") }else{await navigator.clipboard.writeText(url);setShared("Link copied")}};
  if(accepted)return <main className="acceptMoment"><div className="brandMark acceptMark"><BackedMark/></div><p className="eyebrow">PROMISE ACCEPTED</p><h1>YOU’RE<br/>BACKED<span>.</span></h1><strong>{currencySymbol(invite.currency)}{amount}</strong><h2>behind you.</h2><p>1 person believes in this.</p><button className="button primary" onClick={share}>GET MORE PEOPLE BEHIND YOU <span>↗</span></button>{shared&&<small role="status">{shared}</small>}<div className="accountNext"><span>NEXT</span><p>Complete your account to manage this Promise and post progress.</p><button>CONTINUE WITH EMAIL</button></div></main>;
  return <main className="receivePage"><nav><BackedLogo/><span>PRIVATE INVITE</span></nav><section className="receiveIntro"><p className="eyebrow">{invite.backerName.toUpperCase()} BACKED YOU<span>.</span></p><h1>{invite.backerName} believes<br/>you can:</h1></section><section className="receivePromise"><p>YOUR PROMISE</p><h2>{invite.promiseTitle.toUpperCase()}</h2><span>BEFORE {invite.deadline.toUpperCase()}</span></section><section className="receiveMoney"><strong>{currencySymbol(invite.currency)}{amount}</strong><span>is behind you.</span></section>{invite.message&&<blockquote>“{invite.message}”<cite>— {invite.backerName}</cite></blockquote>}<footer><button className="notNow">NOT NOW</button><button className="button primary" disabled={busy} onClick={accept}>{busy?"ACCEPTING…":"I’M IN"}<span>→</span></button></footer></main>}
