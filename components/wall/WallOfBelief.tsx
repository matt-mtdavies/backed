"use client";
import { useState } from "react";
type Backer={readonly name:string;readonly amount:number;readonly message:string;readonly tone:string};
export function WallOfBelief({backers}:{backers:readonly Backer[]}) { const [active,setActive]=useState<Backer|null>(null); return <section className="wall"><div className="sectionTitle"><p className="eyebrow">THE WALL OF BELIEF</p><span>{backers.length} PEOPLE</span></div><div className="portraits">{backers.map((backer,index)=><button style={{"--i":index} as React.CSSProperties} className={`portrait ${backer.tone}`} key={backer.name} onClick={()=>setActive(backer)} aria-label={`View ${backer.name}'s backing`}><b>{backer.name[0]}</b><span>{backer.name}</span></button>)}</div>{active&&<div className="backerDetail"><button onClick={()=>setActive(null)} aria-label="Close">×</button><p>{active.name.toUpperCase()}</p><strong>${active.amount} behind Jason</strong><blockquote>“{active.message}”</blockquote><small>BACKED 42 DAYS AGO.</small></div>}</section> }
