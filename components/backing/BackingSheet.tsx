"use client";
import { useState } from "react";
export function BackingSheet({name}:{name:string}) { const [amount,setAmount]=useState(50); const [done,setDone]=useState(false); if(done) return <div className="confirmed" role="status"><p>YOU’RE BEHIND {name.toUpperCase()}.</p><strong>${amount}</strong><span>Belief made tangible.</span><button onClick={()=>setDone(false)}>DONE</button></div>; return <form className="backingForm" onSubmit={(event)=>{event.preventDefault();setDone(true)}}><p className="eyebrow">GET BEHIND {name.toUpperCase()}</p><h2>How much<br/>do you believe?</h2><div className="amounts" aria-label="Backing amount">{[25,50,100,250].map(value=><button aria-pressed={amount===value} onClick={()=>setAmount(value)} type="button" key={value}>${value}</button>)}</div><label>Say something they’ll need.<textarea maxLength={280} placeholder="You’ve got this."/></label><p className="commitmentNote">Alpha demo — no payment is collected.</p><button className="button primary submit" type="submit">BACK {name.toUpperCase()} ${amount}</button></form> }
