import type { Metadata } from "next"; import { demoPromise } from "@/lib/demo"; import { BackedLogo } from "@/components/brand/BackedLogo"; import { WallOfBelief } from "@/components/wall/WallOfBelief"; import Link from "next/link";
export const metadata: Metadata={ title:"Jason is running his first half marathon | BACKED",description:"$1,100 behind Jason. 5 people are behind him.",robots:{index:false,follow:false},openGraph:{title:"Jason is running his first half marathon",description:"$1,100 behind Jason. 5 people are behind him."},twitter:{card:"summary_large_image",title:"Jason is running his first half marathon",description:"$1,100 behind Jason. 5 people are behind him."}};
export default function PromisePage(){ const p=demoPromise; return <main className="promisePage"><header className="promiseNav"><BackedLogo/><span>JASON’S PROMISE</span></header><section className="promiseHero"><p className="eyebrow">RUNNING · JUNE 30</p><h1>RUN MY FIRST<br/>HALF MARATHON</h1><div className="promiseStats"><div><strong>${p.total.toLocaleString()}</strong><span>behind Jason.</span></div><div className="progressRing" aria-label={`${p.progress}% progress`}><b>{p.progress}%</b><span>PROGRESS</span></div></div><p className="beliefCount"><b>{p.backers.length} people</b> are behind Jason.</p><Link className="button primary stickyCta" href={`/p/${p.slug}/back`}>GET BEHIND JASON <span>↗</span></Link></section><WallOfBelief backers={p.backers}/><section className="updates"><p className="eyebrow">THE WORK</p>{p.updates.map((update,index)=><article key={update}><span>0{index+1}</span><h2>{update}</h2><small>{index===0?"10.0 KM · 54:12":"18.2 KM · 1:44:23"}</small></article>)}</section></main> }
